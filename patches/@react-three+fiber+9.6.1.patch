diff --git a/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js b/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
index 3376d70..584cda3 100644
--- a/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
+++ b/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
@@ -54,6 +54,49 @@ const act = React__namespace['act' + ''];
 const isOrthographicCamera = def => def && def.isOrthographicCamera;
 const isRef = obj => obj && obj.hasOwnProperty('current');
 const isColorRepresentation = value => value != null && (typeof value === 'string' || typeof value === 'number' || value.isColor);
+function createTimerClock(autoStart = true) {
+  const timer = new THREE__namespace.Timer();
+  if (typeof document !== 'undefined') timer.connect(document);
+  return {
+    autoStart,
+    startTime: 0,
+    oldTime: 0,
+    elapsedTime: 0,
+    running: false,
+    start(timestamp) {
+      const now = timestamp ?? performance.now();
+      this.startTime = now;
+      this.oldTime = now;
+      this.elapsedTime = 0;
+      this.running = true;
+      timer.reset();
+      timer.update(now);
+    },
+    stop() {
+      this.getElapsedTime();
+      this.running = false;
+      this.autoStart = false;
+    },
+    getElapsedTime(timestamp) {
+      this.getDelta(timestamp);
+      return this.elapsedTime;
+    },
+    getDelta(timestamp) {
+      let diff = 0;
+      if (this.autoStart && !this.running) {
+        this.start(timestamp);
+        return 0;
+      }
+      if (this.running) {
+        timer.update(timestamp);
+        diff = timer.getDelta();
+        this.oldTime = timestamp ?? performance.now();
+        this.elapsedTime += diff;
+      }
+      return diff;
+    }
+  };
+}
 
 /**
  * An SSR-friendly useLayoutEffect.
@@ -1038,7 +1081,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createTimerClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
@@ -7868,7 +7911,7 @@ let subscribers;
 let subscription;
 function update(timestamp, state, frame) {
   // Run local effects
-  let delta = state.clock.getDelta();
+  let delta = state.clock.getDelta(timestamp);
 
   // In frameloop='never' mode, clock times are updated using the provided timestamp
   if (state.frameloop === 'never' && typeof timestamp === 'number') {
diff --git a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
index 97d8295..a039f74 100644
--- a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
+++ b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
@@ -29,6 +29,49 @@ const act = React['act' + ''];
 const isOrthographicCamera = def => def && def.isOrthographicCamera;
 const isRef = obj => obj && obj.hasOwnProperty('current');
 const isColorRepresentation = value => value != null && (typeof value === 'string' || typeof value === 'number' || value.isColor);
+function createTimerClock(autoStart = true) {
+  const timer = new THREE.Timer();
+  if (typeof document !== 'undefined') timer.connect(document);
+  return {
+    autoStart,
+    startTime: 0,
+    oldTime: 0,
+    elapsedTime: 0,
+    running: false,
+    start(timestamp) {
+      const now = timestamp ?? performance.now();
+      this.startTime = now;
+      this.oldTime = now;
+      this.elapsedTime = 0;
+      this.running = true;
+      timer.reset();
+      timer.update(now);
+    },
+    stop() {
+      this.getElapsedTime();
+      this.running = false;
+      this.autoStart = false;
+    },
+    getElapsedTime(timestamp) {
+      this.getDelta(timestamp);
+      return this.elapsedTime;
+    },
+    getDelta(timestamp) {
+      let diff = 0;
+      if (this.autoStart && !this.running) {
+        this.start(timestamp);
+        return 0;
+      }
+      if (this.running) {
+        timer.update(timestamp);
+        diff = timer.getDelta();
+        this.oldTime = timestamp ?? performance.now();
+        this.elapsedTime += diff;
+      }
+      return diff;
+    }
+  };
+}
 
 /**
  * An SSR-friendly useLayoutEffect.
@@ -1013,7 +1056,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: createTimerClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
@@ -16040,7 +16083,7 @@ let subscribers;
 let subscription;
 function update(timestamp, state, frame) {
   // Run local effects
-  let delta = state.clock.getDelta();
+  let delta = state.clock.getDelta(timestamp);
 
   // In frameloop='never' mode, clock times are updated using the provided timestamp
   if (state.frameloop === 'never' && typeof timestamp === 'number') {
diff --git a/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js b/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
index 1b04ea6..c54ef13 100644
--- a/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
+++ b/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
@@ -54,6 +54,49 @@ const act = React__namespace['act' + ''];
 const isOrthographicCamera = def => def && def.isOrthographicCamera;
 const isRef = obj => obj && obj.hasOwnProperty('current');
 const isColorRepresentation = value => value != null && (typeof value === 'string' || typeof value === 'number' || value.isColor);
+function createTimerClock(autoStart = true) {
+  const timer = new THREE__namespace.Timer();
+  if (typeof document !== 'undefined') timer.connect(document);
+  return {
+    autoStart,
+    startTime: 0,
+    oldTime: 0,
+    elapsedTime: 0,
+    running: false,
+    start(timestamp) {
+      const now = timestamp ?? performance.now();
+      this.startTime = now;
+      this.oldTime = now;
+      this.elapsedTime = 0;
+      this.running = true;
+      timer.reset();
+      timer.update(now);
+    },
+    stop() {
+      this.getElapsedTime();
+      this.running = false;
+      this.autoStart = false;
+    },
+    getElapsedTime(timestamp) {
+      this.getDelta(timestamp);
+      return this.elapsedTime;
+    },
+    getDelta(timestamp) {
+      let diff = 0;
+      if (this.autoStart && !this.running) {
+        this.start(timestamp);
+        return 0;
+      }
+      if (this.running) {
+        timer.update(timestamp);
+        diff = timer.getDelta();
+        this.oldTime = timestamp ?? performance.now();
+        this.elapsedTime += diff;
+      }
+      return diff;
+    }
+  };
+}
 
 /**
  * An SSR-friendly useLayoutEffect.
@@ -1038,7 +1081,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createTimerClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
@@ -16065,7 +16108,7 @@ let subscribers;
 let subscription;
 function update(timestamp, state, frame) {
   // Run local effects
-  let delta = state.clock.getDelta();
+  let delta = state.clock.getDelta(timestamp);
 
   // In frameloop='never' mode, clock times are updated using the provided timestamp
   if (state.frameloop === 'never' && typeof timestamp === 'number') {
